#include <stdio.h>

/**
 * main - print if the number is positive, zero or negative
 * Return: Always zero (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
