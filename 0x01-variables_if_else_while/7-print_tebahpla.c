#include <stdio.h>

/**
 * main - print if the number is positive, zero or negative
 * Description: using the main function
 * Return: Always Zero
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
